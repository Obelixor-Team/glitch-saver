# Add this line to your ~/.config/fish/config.fish file
# Make sure to replace /path/to/screensaver_config with the actual path
# NOTE: The 'TMOUT' feature is not supported in the fish shell.
# This will source the script, but the timeout will not work.
source /path/to/screensaver_config/saver.sh
